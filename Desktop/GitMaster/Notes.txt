cd ..
ls / 

git clone 
git status
git add // git add -A or git add .
git commit // git commit -M "write your message here"
git push 
git pull
(escape):wq //bcz git uses vi text editior by default and its a vi command
Basically if don't commit with message it prompts multiline message so you can make multiline comments and to escape from it just press escape and :wq